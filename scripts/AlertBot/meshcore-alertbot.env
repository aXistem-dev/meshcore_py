# MeshCore AlertBot Environment Configuration

# Serial device path (default: /dev/ttyUSB0)
# The device will be mapped to /dev/ttyUSB0 inside the container
MESHCORE_DEVICE=/dev/ttyUSB0

# Timezone (optional, defaults to system timezone)
TZ=Europe/Brussels

# Channel index (default: 0 for public channel)
MESHCORE_CHANNEL_IDX=0

# Message interval in seconds (default: 300 - 5 minutes)
MESHCORE_MESSAGE_INTERVAL=300

# Cycle interval in minutes (default: 180 - 3 hours)
MESHCORE_CYCLE_INTERVAL=180

# Messages to send (JSON array format or newline-separated)
# Option 1: JSON array format (use single quotes to avoid escaping issues):
# MESHCORE_MESSAGES='["Message 1","Message 2","Message 3"]'
# Option 2: Newline-separated (easier to edit):
# MESHCORE_MESSAGES="Message 1\nMessage 2\nMessage 3"
# If not set, default messages will be used
MESHCORE_MESSAGES='["📻 Welkom op het MeshCore netwerk van Kortrijk! Je bent op de juiste plek.","🌐 Help ons het netwerk uitbreiden! Meer nodes = beter bereik voor iedereen.","💬 Stuur af en toe een bericht in dit publieke kanaal, zo weten we dat je er bent!","💬 Join onze Discord: https://discord.gg/kvybAgqnhD en chat met gelijkgestemden","🌍 Bezoek https://radio-actief.be - de actiefste radio community van België"]'

